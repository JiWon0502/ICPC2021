#include <stdio.h>
#include <stdlib.h>
int arr = 
int main(){
    int N;
    scanf("%d", &N);
    for(int i = 0; i<N; i++)
        scanf("%ld", &arr[i]);
    qsort(arr, N, sizeof(long int));
    for(int j = N-1; j>0; j--)
        printf("%ld\n", arr[j]);
    return 0;
}

